<%= form_for(@finder) do |f| %>
  <% if @finder.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@finder.errors.count, "error") %> prohibited this finder from being saved:</h2>

      <ul>
      <% @finder.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :photo %><br>
    <%= f.text_field :photo %>
  </div>
  <div class="field">
    <%= f.label :album_id %><br>
    <%= f.number_field :album_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
